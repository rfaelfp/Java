package classe;

public class AreaCirc {
	
	double raio; 
	static final double PI = 3.14; // Constantes devem ser declaradas em maisculo devido a convenção. 
	
	AreaCirc(double raioInicial) {
		raio = raioInicial;	
	}
	
	double area() {
		return pi * Math.pow(raio, 2);
	}
}
